{% extends 'base.html.twig' %}

{% block title %}Modifier Recette{% endblock %}

{% block body %}
    <div class="container">
        {{ include('recette/layout/stepper.html.twig') }}
        <div class="row">
            <div class="col-10">
                <h1 class="text-center">Information</h1>
            </div>
            <div class="col-2">
                <a class="btn float-right" href="{{ path('recette_new_ingredients', {id:recette.id}) }}"><i class="fa fa-2x fa-arrow-right" aria-hidden="true"></i></a>
            </div>
        </div>
        <div class="row mt-5">
            <div class="col">
                {{ include('recette/layout/card_show.html.twig') }}
            </div>
        </div>
        <div class="row mt-5">
            <div class="col">
                {{ form(formRecette) }}
            </div>
        </div>
    </div>
{% endblock %}
